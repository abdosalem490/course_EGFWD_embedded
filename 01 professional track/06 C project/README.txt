the actual application is the "app.exe"
in order to run the application type the following command in the command line
./app.exe

and to build the project just simply type in the command line interface
make all

and to delete the executable file(.exe) simply type in the command line interface 
make clean